#include <stdio.h>
#include <stdlib.h>
#define pi 3.14

int main() {

float radius, area;

printf("Enter the radius of the circle : ");
scanf("%f", &radius);

area = pi * radius * radius;
printf("\nThe area of the circle is: %f", area);

return 0;

}
